# Configuration locale de Streamlit pour l'application Controle Agences.

# Configuration Streamlit — compatible cloud ET local.

[browser]
gatherUsageStats = false

[server]
# Taille maximale d'un fichier televerse (Mo).
maxUploadSize = 50

[theme]
base = "light"
primaryColor = "#1f2d3d"
